{% extends 'base.html.twig' %}

{% block body %}

<div class="general-window card mb-3">
    <h1 class="h3 font-weight-normal card-header text-primary">Ajouter une description</h1>
    <div class="card-body">
    
    {{ form_start(form) }}

    {{ form_row(form.texte) }}
    {{ form_row(form.imageFile, {'label' : 'Ajouter une image ' }) }}
    
    <button type="submit" class="btn btn-success"> Enregistrer </button>
    
    {{ form_end(form)}}
    
    </div>
</div>

{% endblock %}